# PRD 索引

## 当前活跃版本

**{{V1.x}}**——{{版本特性说明，如「单 PRD」/「并行版本含 N 份」/「合并版本」}}

| 场景 | 文件 | 状态 |
|---|---|---|
| {{场景名}} | `{{文件名}}` | {{状态枚举值}} |

## 历史版本链

| 版本 | 档期 | 主交付 | 状态文件 |
|---|---|---|---|
| {{V1.0}} | {{档期}} | {{交付内容}} | `{{文件名}}` |

## 跨版本关系约束

{{描述非线性关系，例如：
- 延后：哪些功能从 V1.x 延后到 V1.y
- 并行：哪些版本同期交付
- 合并：哪些版本合并为大版本
- 命名规则：文件前缀约定
- 废弃：哪些版本标记为废弃，何时归档
}}

## {{可选段落：版本增量文档}}

{{如果有 V1.x.y 增量改动以独立 md 承接（不重写主 PRD），列在此处}}

- `{{增量文档名}}`：{{增量内容一句话}}

## 状态字段枚举（AI patch 时只能从此选）

`A 级定稿` / `B 级` / `C 级` / `草稿` / `已废弃` / `已超出版本`

---

*本索引由 ai-pm-prd skill 自动维护（生成新 PRD / 重命名 / 移动时同步 patch）；AI patch 时只 INSERT 新条目，不 DELETE 旧条目；PM review 后定稿。*
