# 视觉锚点包审计

审计时间：{{ISO8601}}  
请求文件：`request.json`  
交接清单：`manifest.json`  
结论：ready / partial / failed

## 生成范围

| 页面 ID | 页面名称 | 图片 | 状态 | 说明 |
|---|---|---|---|---|
| home | 首页工作台 | `images/01-home.png` | ready | 说明可交接给 HTML 原型的约束 |

## 评分

| 维度 | 分数 | 结论 |
|---|---:|---|
| 视觉一致性 | 0-4 | 是否继承参考图的布局节奏、密度和组件比例 |
| PRD 覆盖 | 0-4 | 是否覆盖关键页面、主流程和关键状态 |
| 文本可读性 | 0-4 | 标题、字段、按钮和信息层级是否可辨认 |
| 业务真实感 | 0-4 | 业务对象、状态和数据是否可信 |

总分：{{N}}/16

判定规则：

- `>= 13` 且单项 `>= 3`：ready
- `10-12` 或单项 `= 2`：partial
- `< 10` 或任一单项 `< 2`：failed

## 可交接给 Claude Code 的约束

- 布局骨架：
- 页面密度：
- 组件形态：
- Agent 状态表达：
- 禁忌项：

## 需要 Claude Code 注意

- 图片中文字只作为视觉表达，不能作为 PRD 字段或最终文案事实源。
- HTML 原型必须可点击、可走主流程，不能用图片替代交互。
- 若 PRD 与图片冲突，以 PRD 正文和用户最新指令为准。

## 已知风险

- 风险 1：
- 风险 2：

## 后续建议

- 若 `ready`：Claude Code 读取 `manifest.json` 和 `visual-fingerprint.md` 后继续 HTML 原型。
- 若 `partial`：补齐缺失页面，或在 HTML 原型审计中标注风险后继续。
- 若 `failed`：降级普通 HTML 原型，重新生成视觉锚点包前不要阻塞主流程。
