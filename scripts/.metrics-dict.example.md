# 事实字典模板：实体 × 权限 × 口径（.metrics-dict.example.md · 入库占位版）

> 真实字典在本机 `scripts/.metrics-dict.md`（gitignore，含内部字段名/产品名，绝不入库——同 `.share-denylist` 先例）。
> 用途：查业务数据 / 写 PRD 涉及业务实体时先查字典；命中直接用；未覆盖明确说"口径未覆盖"，先探测再问，不硬拼 SQL。
> 维护纪律：踩到新口径坑当场补一行；条目带核实日期，超 90 天引用时提示复核。

## 表一 · 指标口径

| # | 口径名 | 表/字段 | 条件 | 已知坑 | 核实 |
|---|---|---|---|---|---|
| 1 | 某功能应用（示例） | some_fact_table | `type IN ('1','2')` | 某标记字段埋点不可靠，别加过滤 | MM-DD |
| 2 | 学期写法（示例） | term_name | `'YYYY-YYYY学年第X学期'` | 与工具默认值不同，先 GROUP BY 确认实际值 | MM-DD |

## 表二 · 业务实体与权限线

| # | 实体 | 定义 | 权限项/位置 | 常见混淆对 | 核实 |
|---|---|---|---|---|---|
| 1 | 权限线 A（示例） | 新引擎跑在新载体 | 平台某模块，全科 | ≠权限线 B，两条线独立 | MM-DD |
| 2 | 试点校名单（示例） | 总表 N 批次并集去重 | 云表 token + 本地落盘路径 | ≠另一条线的清单 | MM-DD |

## 表三 · 同物异名归一（检索归一化用 · Stage4-B2）

> 用途：grep / 搜记忆或语料前先扩词，防「同一物不同叫法搜不到」。脚本 `python3 scripts/entity-expand.py "<词>" [--grep]`。
> ⚠️ 只放「确定同一物的不同叫法」（宁窄勿宽）；「该区分别搞混」写备注、不当别名并入。

| 规范名 | 别名 / 异名 / 缩写（顿号分隔） | 备注（别错并的边界） |
|---|---|---|
| 权限项 A（示例） | 别名1、别名2、缩写 | =某定义；**≠权限项 B**（载体/维度不同别并入） |

## 补一行区（踩到新坑当场追加）
