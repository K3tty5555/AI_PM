# AI PM v0.4.0 Wave 1 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 13 项独立改动（全部可并行），为后续架构重组和大功能奠定基础。

**Architecture:** Wave 1 的所有任务互不依赖，可并行或按任意顺序执行。每个任务是独立文件的新建或小范围修改。技能侧改动不涉及客户端编译，客户端改动通过 `npm run build` 验证。

**Tech Stack:** Markdown Skills, JSON configs, Rust/Tauri, React/TypeScript/Tailwind

**Design Doc:** `docs/plans/2026-03-28-v0.4.0-iteration-design.md`

**基线校准结果（2026-03-28 实际盘点）：**
```
.claude/skills/ai-pm/
├── SKILL.md          # 558 行，含所有 Phase 流程 + 交互 + 边缘情况
├── doctor.md         # 72 行
├── illustration.md   # AI 流程图生成
├── instinct.md       # 自学习系统
└── web-analysis.md   # 57 行

templates/
├── prd-styles/default/   # feishu-template.md, pdf-style.css, style-config.json
├── knowledge-base/       # 6 个子目录
└── ui-specs/             # 2 个规范

❌ 不存在：phase-workflows.md, user-interaction.md, edge-cases.md, templates/configs/, templates/presets/
```

---

### Task 1: 基线校准 — 更新 MEMORY.md

**Files:**
- Modify: `/Users/xiaowu/.claude/projects/-Users-xiaowu-workplace-AI-PM/memory/MEMORY.md`

**Step 1: 更新 MEMORY.md 中过时的架构决策记录**

找到"2026-03-02: ai-pm/SKILL.md 拆分"相关段落，更新为当前实际状态：SKILL.md 已回归为 558 行单文件（未拆分），`phase-workflows.md` / `user-interaction.md` / `edge-cases.md` 不存在。标注 v0.4.0 Wave 2 将重新执行拆分。

**Step 2: 提交**

```bash
git add /Users/xiaowu/.claude/projects/-Users-xiaowu-workplace-AI-PM/memory/MEMORY.md
git commit -m "chore: update MEMORY.md — correct ai-pm skill structure baseline"
```

---

### Task 2: 创建 templates/presets/ 目录 + 行业预设

**Files:**
- Create: `templates/presets/industry-style-presets.json`

**Step 1: 创建目录和文件**

```bash
mkdir -p /Users/xiaowu/workplace/AI_PM/templates/presets
```

**Step 2: 写入行业预设 JSON**

创建 `templates/presets/industry-style-presets.json`：

```json
{
  "general": {
    "label": "通用/不指定",
    "accent": "#374151",
    "bg": "#F9FAFB",
    "font": "system",
    "keywords": "中性、灵活"
  },
  "finance": {
    "label": "金融/法律",
    "accent": "#1E3A5F",
    "bg": "#F5F5F0",
    "font": "serif",
    "keywords": "权威、稳重"
  },
  "healthcare": {
    "label": "医疗/健康",
    "accent": "#0D7377",
    "bg": "#F0F9F4",
    "font": "sans-serif",
    "keywords": "信任、洁净"
  },
  "tech": {
    "label": "科技/互联网",
    "accent": "#4F46E5",
    "bg": "#F8FAFC",
    "font": "sans-serif",
    "keywords": "创新、简洁"
  },
  "education": {
    "label": "教育",
    "accent": "#1B4F72",
    "bg": "#FDF6EC",
    "font": "mixed",
    "keywords": "严谨、亲和"
  },
  "ecommerce": {
    "label": "电商/零售",
    "accent": "#DC2626",
    "bg": "#FFFBEB",
    "font": "bold-sans",
    "keywords": "活力、促销感"
  },
  "enterprise": {
    "label": "企业内部工具",
    "accent": "#374151",
    "bg": "#F9FAFB",
    "font": "system",
    "keywords": "克制、高效"
  }
}
```

**Step 3: 提交**

```bash
cd /Users/xiaowu/workplace/AI_PM
git add -f templates/presets/industry-style-presets.json
git commit -m "feat(config): add industry style presets — 7 industries with color/font/keywords"
```

---

### Task 3: 创建文案框架模板

**Files:**
- Create: `templates/presets/copywriting-frameworks.md`

**Step 1: 写入文案框架**

创建 `templates/presets/copywriting-frameworks.md`，内容包含：
- AIDA 框架（Attention → Interest → Desire → Action）+ prompt 模板
- PAS 框架（Problem → Agitate → Solution）+ prompt 模板
- FAB 框架（Feature → Advantage → Benefit）+ prompt 模板
- CTA 公式：`[动作动词] + [用户得到什么] + [紧迫/简单]`
- 异议处理模板（太贵/没用/没时间/怕失败 → 回应 + 放置位置）
- 注入点标注：Phase 3 → AIDA，Phase 5 → FAB，Phase 8 → CTA 审计

**Step 2: 提交**

```bash
cd /Users/xiaowu/workplace/AI_PM
git add -f templates/presets/copywriting-frameworks.md
git commit -m "feat(config): add copywriting frameworks — AIDA, PAS, FAB, CTA, objection handling"
```

---

### Task 4: 创建样例 PRD 文件（3 个）

**Files:**
- Create: `templates/prd-styles/default/sample-b2b-saas.md`
- Create: `templates/prd-styles/default/sample-consumer-app.md`
- Create: `templates/prd-styles/default/sample-internal-tool.md`

**Step 1: 读取现有 PRD 模板和已有项目作为参照**

先读取 `templates/prd-styles/default/feishu-template.md` 了解字段结构。
再读取 `output/projects/` 中一个已完成项目的 PRD（如 `web端考试阅卷/05-prd/05-PRD-v1.0.md`）了解实际质量。

**Step 2: 创建 B2B SaaS 样例**

`sample-b2b-saas.md`（300-500 行），模拟一个 B 端 SaaS 产品的完整 PRD。字段结构严格按 feishu-template.md，内容质量对标已有项目。

**Step 3: 创建 C 端 App 样例**

`sample-consumer-app.md`，模拟一个轻量 C 端产品。

**Step 4: 创建内部工具样例**

`sample-internal-tool.md`，模拟企业内部管理系统。

**Step 5: 提交**

```bash
cd /Users/xiaowu/workplace/AI_PM
git add templates/prd-styles/default/sample-*.md
git commit -m "feat(templates): add 3 sample PRDs — B2B SaaS, consumer app, internal tool"
```

---

### Task 5: 重写症状索引

**Files:**
- Modify: `.claude/skills/ai-pm/SKILL.md`（找到边缘情况/异常处理相关段落重写为症状索引）

**Step 1: 读取当前 SKILL.md**

读取 `.claude/skills/ai-pm/SKILL.md` 全文，找到处理边缘情况、异常、用户请求偏离的段落。

**Step 2: 将这些段落重组为症状驱动索引格式**

```markdown
## 常见场景速查

| 用户说的话 | 处理方式 |
|-----------|---------|
| "PRD 太长了" | 帮用户按模块拆分，建议分文档管理 |
| "原型和 PRD 对不上" | 帮用户检查原型和 PRD 是否对齐（触发审计） |
| "上次做到哪了" | 读取 _status.json 恢复到上次阶段 |
| "不想做竞品研究" | 可以跳过，记录跳过原因 |
| "换个风格" | 重新选择 PRD 风格或原型设计规范 |
| "需求改了" | 回到分析阶段，标记变更点 |
| "生成的 PRD 质量不好" | 检查样例对照，调整生成参数 |
| "导出格式有问题" | 检查排版配方和字体可用性 |
| "评审意见太多" | 帮用户按严重程度排序，逐项决定 |
```

**Step 3: 验证 SKILL.md 语法正确（无 broken links）**

**Step 4: 提交**

```bash
cd /Users/xiaowu/workplace/AI_PM
git add .claude/skills/ai-pm/SKILL.md
git commit -m "feat(skill): rewrite edge cases as symptom-driven index in SKILL.md"
```

---

### Task 6: 占位符扫描 Rust 命令

**Files:**
- Modify: `app/src-tauri/src/commands/files.rs`（追加 scan_placeholders 函数）
- Modify: `app/src-tauri/src/lib.rs`（注册命令）
- Modify: `app/src/lib/tauri-api.ts`（添加类型和 API）

**Step 1: 在 files.rs 末尾追加**

```rust
#[derive(Debug, Serialize)]
#[serde(rename_all = "camelCase")]
pub struct PlaceholderMatch {
    pub line: usize,
    pub column: usize,
    pub matched_text: String,
    pub context: String,
    pub rule_name: String,
}

#[tauri::command]
pub fn scan_placeholders(
    state: tauri::State<'_, AppState>,
    project_id: String,
) -> Result<Vec<PlaceholderMatch>, String> {
    // 读取项目 PRD markdown
    // 用正则扫描：[待补充], [此处填写], TBD, TODO, Lorem ipsum,
    // placeholder, [Feature Name], [产品名], [公司名], xxxx, FIXME
    // 排除 Markdown 链接中的方括号 [text](url)
    // 返回匹配列表
}
```

**Step 2: 在 lib.rs invoke_handler 中注册**

**Step 3: 在 tauri-api.ts 添加类型和 api 方法**

```typescript
export interface PlaceholderMatch {
  line: number
  column: number
  matchedText: string
  context: string
  ruleName: string
}

// api 对象中
scanPlaceholders: (projectId: string) =>
  safeInvoke<PlaceholderMatch[]>("scan_placeholders", { projectId }),
```

**Step 4: 验证**

```bash
cd /Users/xiaowu/workplace/AI_PM/app/src-tauri && cargo check
cd /Users/xiaowu/workplace/AI_PM/app && npx tsc --noEmit
```

**Step 5: 提交**

```bash
cd /Users/xiaowu/workplace/AI_PM
git add app/src-tauri/src/commands/files.rs app/src-tauri/src/lib.rs app/src/lib/tauri-api.ts
git commit -m "feat(backend): add scan_placeholders command for export preflight"
```

---

### Task 7: 前置检查清单 Rust 命令

**Files:**
- Modify: `app/src-tauri/src/commands/projects.rs`（追加 check_phase_prerequisites）
- Modify: `app/src-tauri/src/lib.rs`
- Modify: `app/src/lib/tauri-api.ts`

**Step 1: 实现 check_phase_prerequisites**

```rust
#[derive(Debug, Serialize)]
#[serde(rename_all = "camelCase")]
pub struct PrerequisiteItem {
    pub id: String,
    pub label: String,
    pub passed: bool,
    pub check_type: String,  // "auto" | "manual"
    pub hint: Option<String>,
    pub navigate_to: Option<String>,
}

#[tauri::command]
pub fn check_phase_prerequisites(
    state: tauri::State<'_, AppState>,
    project_id: String,
    phase_id: String,
) -> Result<Vec<PrerequisiteItem>, String> {
    // phase_id == "prd" → 检查需求分析/竞品/故事文件存在（glob 匹配）
    // phase_id == "prototype" → 检查 PRD 存在
    // 其他 phase → 返回空列表
}
```

**Step 2: 注册命令 + 前端类型**

**Step 3: 验证编译**

**Step 4: 提交**

```bash
git commit -m "feat(backend): add check_phase_prerequisites command"
```

---

### Task 8: 行业字段 — 数据库 migration + Rust 命令扩展

**Files:**
- Modify: `app/src-tauri/src/db.rs`（migration version +1，添加 industry 列）
- Modify: `app/src-tauri/src/commands/projects.rs`（create_project / get_project 扩展）
- Modify: `app/src/lib/tauri-api.ts`（Industry 类型 + 接口扩展）

**Step 1: 在 db.rs 添加 migration**

找到当前 schema version（应该是 3 或 4），添加下一个版本的 migration：

```rust
// Version N+1: Add industry column
if current_version < N+1 {
    conn.execute("ALTER TABLE projects ADD COLUMN industry TEXT DEFAULT 'general'", [])?;
    conn.execute("UPDATE schema_version SET version = ?1", [N+1])?;
}
```

**Step 2: 在 create_project 和 get_project 中处理 industry 字段**

**Step 3: 在 tauri-api.ts 添加 Industry 类型**

```typescript
export type Industry = "general" | "finance" | "healthcare" | "tech" | "education" | "ecommerce" | "enterprise"
```

扩展 ProjectSummary/ProjectDetail 接口添加 `industry: Industry`。

**Step 4: 验证编译**

**Step 5: 提交**

```bash
git commit -m "feat(backend): add industry field to projects — migration, create, get"
```

---

### Task 9: 新建项目 Dialog — 行业选择

**Files:**
- Modify: `app/src/components/new-project-dialog.tsx`

**Step 1: 添加行业下拉**

在项目名称和项目类型之后，添加行业选择下拉。7 个选项（含"通用/不指定"默认值），每项左侧 16px 色块圆点。placeholder："选择行业（可选，影响配色推荐）"。

读取 `industry-style-presets.json`（通过 Rust 命令或硬编码到前端）来渲染选项。

**Step 2: 传入 create_project 调用**

**Step 3: 验证构建**

```bash
cd /Users/xiaowu/workplace/AI_PM/app && npm run build
```

**Step 4: 提交**

```bash
git commit -m "feat(frontend): add industry selector to new project dialog"
```

---

### Task 10: mermaid-utils.ts 提取

**Files:**
- Create: `app/src/lib/mermaid-utils.ts`
- Modify: `app/src/pages/tools/Illustration.tsx`（改为引用 mermaid-utils）
- Modify: `app/src/pages/project/Prd.tsx`（改为引用 mermaid-utils，如果有重复定义）

**Step 1: 创建 mermaid-utils.ts**

```typescript
export function detectMermaidType(code: string): string {
  const firstLine = code.trim().split("\n")[0].trim()
  for (const type of ["sequenceDiagram", "flowchart", "classDiagram", "graph"]) {
    if (firstLine.startsWith(type)) return type
  }
  return "graph"
}

export const STYLE_RECOMMENDATIONS: Record<string, { layout: string; style: string; label: string }> = {
  graph:           { layout: "linear-progression", style: "corporate-memphis", label: "线性流程 × 扁平商务" },
  flowchart:       { layout: "linear-progression", style: "corporate-memphis", label: "线性流程 × 扁平商务" },
  sequenceDiagram: { layout: "linear-progression", style: "technical-schematic", label: "线性流程 × 技术图示" },
  classDiagram:    { layout: "structural-breakdown", style: "technical-schematic", label: "层级结构 × 技术图示" },
}

export interface MermaidBlock { ... }

export function extractMermaidBlocks(markdown: string): MermaidBlock[] { ... }
```

**Step 2: 更新 Illustration.tsx 和 Prd.tsx 引用**

删除各自的重复定义，改为 `import { detectMermaidType, STYLE_RECOMMENDATIONS, extractMermaidBlocks } from "@/lib/mermaid-utils"`。

**Step 3: 验证构建**

**Step 4: 提交**

```bash
git commit -m "refactor(frontend): extract mermaid-utils.ts — deduplicate detection and style logic"
```

---

### Task 11: doctor 命令增强

**Files:**
- Modify: `.claude/skills/ai-pm/doctor.md`

**Step 1: 读取当前 doctor.md 内容**

**Step 2: 追加新检查项**

在现有检查项列表后追加：
- 技能描述是否包含触发条件（搜索"当用户说"或 triggers 关键词）
- 技能文件超 500 行警告
- 模板文件可读性（templates/ 下关键文件可访问）
- 样例 PRD 与 feishu-template.md 字段一致性
- 硬编码密钥扫描（精确正则 `sk-[a-zA-Z0-9]{20,}`、`AKIA[0-9A-Z]{16}`，排除 docs/ 和 MEMORY.md）
- 行业预设完整性（JSON 含 7 个行业）
- SKILL.md 路由表引用的文件路径存在性

**Step 3: 提交**

```bash
git commit -m "feat(skill): enhance doctor command — 7 new check items"
```

---

### Task 12: 统一导出预检 Dialog

**Files:**
- Create: `app/src/components/export-preflight-dialog.tsx`

**Step 1: 读取现有 sensitive-scan-dialog.tsx 和 mermaid-render-dialog.tsx 了解模式**

**Step 2: 创建统一 Dialog**

合并敏感信息 + 占位符 + Mermaid 三类检测结果到一个 Dialog 中。用 Tab 或分区展示，只显示有问题的类别。底部统一"返回编辑"/"导出"。

```typescript
interface ExportPreflightDialogProps {
  open: boolean
  sensitiveMatches: SensitiveMatch[]
  placeholderMatches: PlaceholderMatch[]
  mermaidBlocks: MermaidBlock[]
  onExport: (options: ExportPreflightOptions) => void
  onCancel: () => void
}

interface ExportPreflightOptions {
  redact: boolean
  mermaidChoices: Record<number, "ai" | "local" | "skip">
  aiStyles: Record<number, { layout: string; style: string }>
}
```

**Step 3: 验证构建**

**Step 4: 提交**

```bash
git commit -m "feat(frontend): unified ExportPreflightDialog — sensitive + placeholder + mermaid"
```

---

### Task 13: useExportPipeline hook + Prd.tsx 集成

**Files:**
- Create: `app/src/hooks/use-export-pipeline.ts`
- Modify: `app/src/pages/project/Prd.tsx`

**Step 1: 创建 useExportPipeline hook**

状态机管理导出流程：
```typescript
type PipelineState = "idle" | "scanning" | "preflight" | "formatSelect" | "exporting" | "done" | "error"

export function useExportPipeline(projectId: string) {
  // 管理所有导出相关���态
  // startExport(format) → scanning → preflight dialog → format dialog → exporting → done
  // 替代 Prd.tsx 中所有 withSensitiveScan / mermaid / placeholder 相关的 ~15 个 useState
  return {
    state, sensitiveMatches, placeholderMatches, mermaidBlocks,
    preflightOpen, formatSelectOpen,
    startExport, confirmPreflight, confirmFormat, cancel, reset
  }
}
```

**Step 2: 重构 Prd.tsx**

将导出相关的 useState 和 callbacks 迁移到 useExportPipeline，Prd.tsx 只调用 hook 返回值。同时用 ExportPreflightDialog 替代现有的 SensitiveScanDialog + MermaidRenderDialog。

**Step 3: 验证构建**

**Step 4: 提交**

```bash
git commit -m "refactor(frontend): extract useExportPipeline hook, simplify Prd.tsx export flow"
```

---

## 任务依赖

```
Wave 1 全部无依赖，可任意顺序或并行：
  Task 1  (基线校准)
  Task 2  (行业预设 JSON)
  Task 3  (文案框架)
  Task 4  (样例 PRD × 3)
  Task 5  (症状索引)
  Task 6  (占位符扫描命令)
  Task 7  (前置��查命令)
  Task 8  (industry 字段 migration)
  Task 9  (行业选择 Dialog) ← Task 8
  Task 10 (mermaid-utils 提取)
  Task 11 (doctor 增强)
  Task 12 (统一预检 Dialog) ← Task 6
  Task 13 (useExportPipeline) ← Task 12

唯一串行：Task 8 → Task 9，Task 6 → Task 12 → Task 13
其余全部可并行。
```

## 提交策略

每个 Task 单独提交，commit message 格式 `feat/refactor(scope): 简短描述`。
