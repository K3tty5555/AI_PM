# AI PM v0.4.0 Wave 2 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 架构重组 + 导出质感提升 + 补齐 Wave 1 遗留客户端 UI，共 8 个 Task。

**Architecture:** Wave 2 有串行依赖链。Task 1（SKILL.md 拆分）是架构变更，必须先完成。Task 2-4 是 Wave 1 遗留 UI。Task 5-7 是导出增强，依赖 Task 1 完成。Task 8 是收尾验证。

**Tech Stack:** Markdown Skills, JSON configs, Rust/Tauri, React/TypeScript/Tailwind, Python (md2docx.py)

**Design Doc:** `docs/plans/2026-03-28-v0.4.0-iteration-design.md` §2.1-2.3

**Wave 1 完成后基线（2026-03-28）：**
```
.claude/skills/ai-pm/
├── SKILL.md          # 575 行（含症状索引 + Anti-Pattern）
├── doctor.md         # 85 行（22 项检查）
├── illustration.md
├── instinct.md
└── web-analysis.md

templates/presets/
├── industry-style-presets.json    # 7 行业
└── copywriting-frameworks.md      # AIDA/PAS/FAB/CTA

app/ 新增：
├── src/lib/mermaid-utils.ts
├── src/hooks/use-export-pipeline.ts
├── src/components/export-preflight-dialog.tsx
└── src/lib/tauri-api.ts (PlaceholderMatch, PrerequisiteItem, Industry 类型)

Rust 新增命令：scan_placeholders, check_phase_prerequisites
DB schema version: 4 (industry 列)
```

---

### Task 1: 三层知识架构 — SKILL.md 拆分 + Phase 文件 + 路由表

**Files:**
- Modify: `.claude/skills/ai-pm/SKILL.md`（从 575 行缩减到 ~120 行路由表）
- Create: `.claude/skills/ai-pm/phases/phase-0-office-hours.md`
- Create: `.claude/skills/ai-pm/phases/phase-1-requirement.md`
- Create: `.claude/skills/ai-pm/phases/phase-2-analysis.md`
- Create: `.claude/skills/ai-pm/phases/phase-3-research.md`
- Create: `.claude/skills/ai-pm/phases/phase-4-stories.md`
- Create: `.claude/skills/ai-pm/phases/phase-5-prd.md`
- Create: `.claude/skills/ai-pm/phases/phase-6-analytics.md`
- Create: `.claude/skills/ai-pm/phases/phase-7-prototype.md`
- Create: `.claude/skills/ai-pm/phases/phase-8-review.md`
- Create: `.claude/skills/ai-pm/phases/phase-9-retrospective.md`
- Create: `.claude/skills/ai-pm/references/user-interaction.md`
- Create: `.claude/skills/ai-pm/references/symptom-index.md`

**Step 1: Git tag 快照**

```bash
git tag v0.4.0-pre-restructure
```

**Step 2: 创建目录结构**

```bash
mkdir -p .claude/skills/ai-pm/phases
mkdir -p .claude/skills/ai-pm/references
```

**Step 3: 读取当前 SKILL.md，拆分内容到 Phase 文件**

读取 SKILL.md 全文，按以下规则拆分：

- `## 阶段流程` 下的总览图 → 保留在 SKILL.md 路由表
- `### Phase 0: 需求速评` → `phases/phase-0-office-hours.md`
- `### Phase 2+3 并行执行方式` → `phases/phase-2-analysis.md` + `phases/phase-3-research.md`
- `### PRD 生成前确认节点` → `phases/phase-5-prd.md`
- `### Phase 7.5: 原型完整性审计` → `phases/phase-7-prototype.md`
- `## 多代理调度逻辑` → `references/user-interaction.md`
- `## 项目路径解析` → `references/user-interaction.md`
- `## 记忆迁移与备份` → `references/user-interaction.md`
- `## _status.json 规范` → `references/user-interaction.md`
- `## 启动界面逻辑` → `references/user-interaction.md`
- `## 快捷指令` → `references/user-interaction.md`
- `## 有现成文档时的处理` → `references/user-interaction.md`
- `## 常见场景速查` → `references/symptom-index.md`
- `## Anti-Pattern` → `references/symptom-index.md`

没有独立 Phase 段落的阶段（Phase 1/4/6/8/9）创建简短文件，内容为：阶段名 + 输入/输出 + 关键规则引用。

**Step 4: 重写 SKILL.md 为路由表（~120 行）**

保留 frontmatter + 角色定义 + 命令路由表 + 项目目录结构 + 阶段流程总览图。所有详细内容改为 `→ 读 phases/phase-N-xxx.md` 或 `→ 读 references/xxx.md` 的指引。

增加 fallback 逻辑注释：
```markdown
<!-- 兼容性：若 Phase 文件不存在，尝试从本文件查找旧格式段落 -->
```

**Step 5: 运行 doctor 验证**

手动检查路由表中引用的所有 Phase 文件和 reference 文件是否存在。

**Step 6: 提交**

```bash
git add .claude/skills/ai-pm/
git commit -m "refactor(skill): restructure SKILL.md into 3-tier architecture — router + phases + references"
```

---

### Task 2: 样例 PRD 参考 — Rust 命令 + Dialog UI

**Files:**
- Modify: `app/src-tauri/src/commands/templates.rs`（追加 list_prd_samples / get_prd_sample_content）
- Modify: `app/src-tauri/src/lib.rs`（注册命令）
- Modify: `app/src/lib/tauri-api.ts`（添加类型和 API）
- Create: `app/src/components/sample-prd-dialog.tsx`
- Modify: `app/src/pages/project/Prd.tsx`（添加"参考样例"按钮）

**Step 1: Rust 命令 — list_prd_samples**

在 templates.rs 末尾追加：

```rust
#[derive(Debug, Serialize)]
#[serde(rename_all = "camelCase")]
pub struct PrdSample {
    pub id: String,      // "b2b-saas" | "consumer-app" | "internal-tool"
    pub label: String,
    pub industry: String,
    pub line_count: usize,
}

#[tauri::command]
pub fn list_prd_samples(state: State<'_, AppState>) -> Result<Vec<PrdSample>, String> {
    // 扫描 {projects_dir}/../templates/prd-styles/default/sample-*.md
    // 白名单策略：只匹配 sample-*.md
    // 从文件名提取 id，从文件前 5 行提取 # 标题作为 label
    // 返回列表
}

#[tauri::command]
pub fn get_prd_sample_content(
    state: State<'_, AppState>,
    sample_id: String,
) -> Result<String, String> {
    // 白名单验证 sample_id 只含 [a-z0-9-]
    // 读取 sample-{id}.md 全文返回
}
```

**Step 2: 注册命令到 lib.rs**

**Step 3: tauri-api.ts 添加类型和 API 方法**

```typescript
export interface PrdSample {
  id: string
  label: string
  industry: string
  lineCount: number
}

// api 对象中
listPrdSamples: () => safeInvoke<PrdSample[]>("list_prd_samples"),
getPrdSampleContent: (sampleId: string) => safeInvoke<string>("get_prd_sample_content", { sampleId }),
```

**Step 4: 创建 sample-prd-dialog.tsx**

Master-detail 布局 Dialog：
- 左列：3 个样例，带行业标签色块 + 行数
- 右侧：PrdViewer 预览选中样例（懒加载内容）
- 底部：关闭按钮

**Step 5: Prd.tsx 添加"参考样例"按钮**

在 ExportDropdown 旁边添加按钮，点击打开 SamplePrdDialog。

**Step 6: 验证**

```bash
cd app/src-tauri && cargo check
cd app && npm run build
```

**Step 7: 提交**

```bash
git commit -m "feat: add sample PRD reference dialog — Rust commands + master-detail UI"
```

---

### Task 3: 预检卡片 UI — Prd.tsx + Prototype.tsx

**Files:**
- Create: `app/src/components/preflight-card.tsx`
- Modify: `app/src/pages/project/Prd.tsx`（生成按钮上方添加预检卡片）
- Modify: `app/src/pages/project/Prototype.tsx`（生成按钮上方添加预检卡片）

**Step 1: 创建通用 PreflightCard 组件**

```typescript
interface PreflightCardProps {
  projectId: string
  phaseId: string   // "prd" | "prototype"
  onAllPassed?: () => void
}
```

- 调用 `api.checkPhasePrerequisites(projectId, phaseId)` 获取检查项
- 全部通过：折叠为 Accordion "预检通过 ✅"（可展开查看详情）
- 有未通过：展开显示，每项带跳转链接（navigateTo → 路由跳转）
- auto 类型项显示 ✅/❌ 状态，manual 类型项显示可勾选 checkbox
- 页面 focus 时自动刷新（visibilitychange 事件）

**Step 2: Prd.tsx 集成**

在空态页面（`PhaseEmptyState` 之前）和内容态页面（生成按钮区域）添加 `<PreflightCard projectId={projectId} phaseId="prd" />`。

**Step 3: Prototype.tsx 集成**

在空态页面（设计规范选择器和 `PhaseEmptyState` 之间）添加 `<PreflightCard projectId={projectId} phaseId="prototype" />`。

**Step 4: 验证构建**

**Step 5: 提交**

```bash
git commit -m "feat(frontend): add PreflightCard component — prerequisite checks on Prd and Prototype pages"
```

---

### Task 4: Prd.tsx 子组件拆分

**Files:**
- Create: `app/src/components/prd-toolbar.tsx`
- Create: `app/src/components/prd-export-result.tsx`
- Modify: `app/src/pages/project/Prd.tsx`（提取子组件，减少主组件体积）

**Step 1: 提取 PrdToolbar**

将 Prd.tsx 中的 `ExportDropdown` 内部组件（第 61-143 行）+ 工具栏渲染区域（版本选择、导出按钮、diff 切换等）提取为独立的 `prd-toolbar.tsx`。

Props:
```typescript
interface PrdToolbarProps {
  onCopyMd: () => void
  copied: boolean
  onExportDocx: () => void
  onExportShareHtml: () => void
  exporting: boolean
  versions: string[]
  currentVersion: string
  onVersionChange: (v: string) => void
  diffMode: boolean
  onToggleDiff: () => void
}
```

**Step 2: 提取 PrdExportResult**

将导出结果展示区域（成功/错误提示）提取为 `prd-export-result.tsx`。

Props:
```typescript
interface PrdExportResultProps {
  result: { path: string } | { error: string } | null
  onReset: () => void
  onNavigateSettings: () => void
}
```

**Step 3: 更新 Prd.tsx import 引用**

删除内联组件，改为 import PrdToolbar 和 PrdExportResult。

**Step 4: 验证构建 + 确认行为不变**

**Step 5: 提交**

```bash
git commit -m "refactor(frontend): extract PrdToolbar and PrdExportResult from Prd.tsx"
```

---

### Task 5: PDF 封面模板 JSON + Rust 命令

**Files:**
- Create: `templates/presets/pdf-covers.json`
- Modify: `app/src-tauri/src/commands/files.rs`（追加 list_pdf_covers + export_prd_pdf）
- Modify: `app/src-tauri/src/lib.rs`（注册命令）
- Modify: `app/src/lib/tauri-api.ts`（添加类型和 API）

**Step 1: 创建 pdf-covers.json**

15 种封面模板，每种含：id, label（中文别名）, category, features, css（内联 CSS 片段，用于封面 HTML 渲染）。

分类：通用(fullbleed/minimal/report) / 正式(split/proposal/frame) / 创意(diagonal/stripe/poster/editorial) / 学术(typographic/atmospheric) / 特殊(magazine/darkroom/terminal)

行业推荐映射：
```json
{
  "industryDefaults": {
    "general": "minimal",
    "finance": "split",
    "healthcare": "minimal",
    "tech": "fullbleed",
    "education": "typographic",
    "ecommerce": "poster",
    "enterprise": "report"
  }
}
```

**Step 2: Rust 命令 list_pdf_covers**

```rust
#[tauri::command]
pub fn list_pdf_covers(state: State<'_, AppState>) -> Result<serde_json::Value, String> {
    // 读取 templates/presets/pdf-covers.json 并返回
}
```

**Step 3: Rust 命令 export_prd_pdf**

```rust
#[tauri::command]
pub async fn export_prd_pdf(
    app: AppHandle,
    state: State<'_, AppState>,
    project_id: String,
    cover_style: Option<String>,
    accent_color: Option<String>,
) -> Result<String, String> {
    // 1. 先调用 export_prd_share_html 生成 HTML
    // 2. 用 Chrome headless 将 HTML 转 PDF：
    //    chromium --headless --print-to-pdf={output} --no-pdf-header-footer {html_path}
    // 3. 如果指定了 cover_style，生成封面 HTML 页 → 转 PDF → 合并
    //    合并用 Python: from pypdf import PdfMerger
    // 4. 返回 PDF 路径
}
```

**Step 4: tauri-api.ts 添加类型**

```typescript
export interface PdfCover {
  id: string
  label: string
  category: string
  features: string
}

// api 对象
listPdfCovers: () => safeInvoke<{ covers: PdfCover[]; industryDefaults: Record<string, string> }>("list_pdf_covers"),
exportPrdPdf: (projectId: string, coverStyle?: string, accentColor?: string) =>
  safeInvoke<string>("export_prd_pdf", { projectId, coverStyle, accentColor }),
```

**Step 5: 验证编译**

```bash
cd app/src-tauri && cargo check
cd app && npx tsc --noEmit
```

**Step 6: 提交**

```bash
git commit -m "feat(backend): add PDF export with cover templates — 15 covers, industry defaults"
```

---

### Task 6: PDF 封面选择 Dialog UI

**Files:**
- Create: `app/src/components/pdf-cover-dialog.tsx`
- Modify: `app/src/components/prd-toolbar.tsx`（ExportDropdown 添加"导出 PDF"菜单项）
- Modify: `app/src/pages/project/Prd.tsx`（添加 PDF 导出到 pipeline）

**Step 1: 创建 pdf-cover-dialog.tsx**

智能默认模式：
- 根据项目行业自动推荐封面
- Dialog 显示"推荐封面"大预览 + 一键"使用推荐"
- "更多选择"展开后分类显示（通用/正式/创意/学术/特殊）
- 每个选项用轻量 CSS 预览（色块模拟，非 PNG）+ 中文别名
- ☑"记住选择"勾选

```typescript
interface PdfCoverDialogProps {
  open: boolean
  projectIndustry: string
  onConfirm: (coverStyle: string, accentColor: string, remember: boolean) => void
  onCancel: () => void
}
```

**Step 2: 集成到导出流程**

PrdToolbar ExportDropdown 新增"导出 PDF"菜单项。点击后先走 exportPipeline 预检，通过后弹出 PDF 封面选择 Dialog，确认后调用 `api.exportPrdPdf()`。

**Step 3: 验证构建**

**Step 4: 提交**

```bash
git commit -m "feat(frontend): add PDF cover selection dialog — smart defaults + category browsing"
```

---

### Task 7: DOCX 13 套排版配方

**Files:**
- Create: `templates/presets/docx-recipes.json`
- Modify: `.claude/skills/ai-pm-prd/md2docx.py`（支持 recipe 参数）
- Modify: `app/src-tauri/src/commands/files.rs`（export_prd_docx 接收 recipe 参数）
- Modify: `app/src/lib/tauri-api.ts`（扩展 exportPrdDocx 参数）
- Create: `app/src/components/docx-recipe-dialog.tsx`
- Modify: `app/src/components/prd-toolbar.tsx`（DOCX 导出走配方选择）
- Modify: `app/src/pages/project/Prd.tsx`

**Step 1: 创建 docx-recipes.json**

13 套配方，每套含：
```json
{
  "ModernCorporate": {
    "label": "现代企业",
    "category": "business",
    "pageSize": "A4",
    "margins": { "top": "2.54cm", "bottom": "2.54cm", "left": "3.17cm", "right": "3.17cm" },
    "bodyFont": ["PingFang SC", "Microsoft YaHei", "sans-serif"],
    "titleFont": ["PingFang SC", "Microsoft YaHei", "sans-serif"],
    "bodySize": "11pt",
    "titleSize": "22pt",
    "lineSpacing": "1.5",
    "headerStyle": "accent-underline"
  }
}
```

13 套完整列表：ModernCorporate / AcademicThesis / ExecutiveBrief / ChineseGovernment / IEEE / ACM / APA7 / MLA9 / ChicagoTurabian / SpringerLNCS / Nature / HBR / MinimalProposal

分类：商务通用（ModernCorporate / ExecutiveBrief / MinimalProposal / ChineseGovernment / HBR）/ 学术规范（IEEE / ACM / APA7 / MLA9 / ChicagoTurabian / SpringerLNCS / Nature / AcademicThesis）

行业推荐映射。

**Step 2: 修改 md2docx.py 支持 recipe**

- 新增 `--recipe` 参数接收 recipe JSON 路径或 recipe name
- 启动时检测字体可用性，不可用按 fallback 链降级 + warning
- recipe 控制：字体、字号、行距、页边距、标题样式

```bash
python3 md2docx.py <prd.md> <output.docx> [manifest.json] [--recipe=ModernCorporate] [--recipe-file=docx-recipes.json]
```

**Step 3: Rust export_prd_docx 扩展**

```rust
pub async fn export_prd_docx(
    app: AppHandle,
    state: State<'_, AppState>,
    project_id: String,
    recipe: Option<String>,  // 新增
) -> Result<String, String> {
    // ... 现有逻辑 ...
    if let Some(r) = recipe {
        cmd.arg("--recipe").arg(&r);
        cmd.arg("--recipe-file").arg(&recipe_path);
    }
}
```

**Step 4: 创建 docx-recipe-dialog.tsx**

- 推荐区（行业匹配 1-2 个）+ 商务通用 + 学术规范
- 每项带 label + 简述
- 默认选中推荐项

**Step 5: tauri-api.ts 扩展 exportPrdDocx 参数**

```typescript
exportPrdDocx: (projectId: string, recipe?: string) =>
  safeInvoke<string>("export_prd_docx", { projectId, recipe }),
```

**Step 6: 集成到 PrdToolbar — DOCX 导出走配方选择**

**Step 7: 验证构建**

```bash
cd app/src-tauri && cargo check
cd app && npm run build
```

**Step 8: 提交**

```bash
git commit -m "feat: DOCX 13 recipe system — recipes JSON, md2docx.py recipe support, selection dialog"
```

---

### Task 8: Wave 2 收尾 — doctor 验证 + 构建检查

**Files:**
- Modify: `.claude/skills/ai-pm/doctor.md`（更新路由表路径检查适配新的 phases/ 目录）

**Step 1: 更新 doctor.md 路径检查**

更新第 22 项检查"SKILL.md 路由表路径存在性"——适配新的 `phases/phase-N-xxx.md` 路径格式。

**Step 2: 全量验证**

```bash
# Rust
cd app/src-tauri && cargo check

# TypeScript
cd app && npx tsc --noEmit

# 前端构建
cd app && npm run build
```

**Step 3: 提交**

```bash
git commit -m "chore: Wave 2 finalize — doctor paths update, full build verification"
```

---

## 任务依赖

```
Task 1 (SKILL.md 拆分)          ← 无依赖，但建议最先执行
Task 2 (样例参考 Dialog)         ← 无依赖，可与 Task 1 并行
Task 3 (预检卡片 UI)             ← 无依赖，可与 Task 1 并行
Task 4 (Prd.tsx 子组件拆分)      ← 无依赖，可与 Task 1 并行
Task 5 (PDF 封面 Rust + JSON)   ← Task 4（Prd.tsx 拆分后更容易集成）
Task 6 (PDF 封面 Dialog UI)     ← Task 5
Task 7 (DOCX 配方)              ← Task 4
Task 8 (收尾验证)               ← Task 1, 5, 6, 7

并行组 A: Task 1
并行组 B: Task 2, 3, 4 (可同时进行)
串行链: Task 5 → Task 6
串行链: Task 7
收尾: Task 8 (最后执行)
```

## 提交策略

每个 Task 单独提交，commit message 格式 `feat/refactor(scope): 简短描述`。
